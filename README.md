# LangGraphApp
A simple Flask-based API demonstrating a multi-step LLM workflow using LangGraph. The system processes business data through sequential agents (analysis, risk, strategy, summarization) with shared state management.
